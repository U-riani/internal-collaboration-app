{
  "name": "internal-collaboration-app",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c auto \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "dev:backend": "npm --prefix apps/api run dev",
    "dev:frontend": "npm --prefix apps/web run dev",
    "install:all": "npm ci --prefix apps/api && npm ci --prefix apps/web",
    "build": "npm --prefix apps/web run build",
    "check": "npm --prefix apps/api run check && npm --prefix apps/web run build",
    "setup": "node scripts/setup.mjs",
    "test": "npm --prefix apps/api test && npm --prefix apps/web test",
    "db:generate": "npm --prefix apps/api run db:generate",
    "db:migrate": "npm --prefix apps/api run db:migrate",
    "format": "prettier --write \"apps/*/src/**/*.{js,jsx,css}\" \"apps/api/tests/**/*.js\" \"apps/web/tests/**/*.{js,jsx}\" \"scripts/*.mjs\""
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "prettier": "3.9.7"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
